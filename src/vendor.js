/* vendor libraries here as there is benefit of caching them because they change infrequently
 so there is no need to download them everytime
 
 used by webpack in production build only
 */

/* eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch';
